package br.edu.ifrs.ajudaqui.naoutilizando;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

import androidx.appcompat.app.AppCompatActivity;

import br.edu.ifrs.ajudaqui.R;

public class LogOngActivity extends AppCompatActivity {

   /* @Override
        protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_log_ong);
        Button botaoCria = findViewById(R.id.btnCria);
        Button  botaoLoga = findViewById(R.id.btnLoga);
        botaoCria.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intencao = new Intent(LogOngActivity.this, CadOngActivity.class);
                startActivity(intencao);
            }
        });
        botaoLoga.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intencao = new Intent(LogOngActivity.this, ListOngActivity.class);
                startActivity(intencao);
            }
        });
    }*/
}
