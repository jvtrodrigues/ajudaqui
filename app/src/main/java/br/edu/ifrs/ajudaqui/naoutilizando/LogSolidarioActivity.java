package br.edu.ifrs.ajudaqui.naoutilizando;

import android.content.Intent;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;

import androidx.appcompat.app.AppCompatActivity;

import br.edu.ifrs.ajudaqui.R;

public class LogSolidarioActivity extends AppCompatActivity {

    /*@Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_log_solidario);
        Button botaoCria = findViewById(R.id.btnCriaSol);
        Button  botaoLoga = findViewById(R.id.btnLogaSol);
        botaoCria.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intencao = new Intent(LogSolidarioActivity.this, CadSolidarioActivity.class);
                startActivity(intencao);
            }
        });
        botaoLoga.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View v) {
                Intent intencao = new Intent(LogSolidarioActivity.this, ListSolidarioActivity.class);
                startActivity(intencao);
            }
        });
    }*/
}
